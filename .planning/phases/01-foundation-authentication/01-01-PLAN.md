---
phase: 01-foundation-authentication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: 
  - src/backend/server.ts
  - src/backend/routes/auth.ts
  - src/backend/models/User.ts
  - src/backend/middleware/auth.ts
  - src/backend/utils/db.ts
  - src/backend/config.ts
  - src/frontend/src/lib/stores/auth.ts
  - src/frontend/src/routes/+layout.svelte
  - src/frontend/src/routes/login/+page.svelte
  - src/frontend/src/routes/register/+page.svelte
  - src/frontend/src/routes/logout/+page.svelte
  - src/frontend/package.json
  - src/backend/deps.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - DB-01
  - API-03
  - UI-01
  - UI-02

must_haves:
  truths:
    - "User can register with email and password"
    - "User can log in and maintain a session"
    - "User can log out from any page"
    - "User data is stored securely in the database"
    - "Authentication API endpoints respond correctly"
    - "Frontend has responsive design"
  artifacts:
    - path: "src/backend/routes/auth.ts"
      provides: "Authentication API endpoints"
      exports: ["POST /auth/register", "POST /auth/login", "POST /auth/logout"]
    - path: "src/backend/models/User.ts"
      provides: "User data model and database schema"
      contains: "User interface, database operations"
    - path: "src/frontend/src/lib/stores/auth.ts"
      provides: "Authentication state management"
      exports: ["login", "logout", "auth store"]
    - path: "src/frontend/src/routes/login/+page.svelte"
      provides: "Login form UI"
      min_lines: 30
    - path: "src/frontend/src/routes/register/+page.svelte"
      provides: "Registration form UI"
      min_lines: 30
    - path: "src/backend/middleware/auth.ts"
      provides: "Authentication middleware"
      exports: ["authMiddleware"]
  key_links:
    - from: "src/frontend/src/lib/stores/auth.ts"
      to: "/api/auth/login"
      via: "fetch API call"
      pattern: "fetch\\('/api/auth/login'"
    - from: "src/backend/routes/auth.ts"
      to: "MongoDB users collection"
      via: "database query"
      pattern: "db\\.collection\\('users'\\)"
    - from: "src/backend/middleware/auth.ts"
      to: "JWT verification"
      via: "token validation"
      pattern: "jwtVerify"
---

<objective>
Establish core infrastructure and user authentication system for ClauseVault.

Purpose: Create the foundational authentication system that allows users to register, log in, and maintain secure sessions while storing user data securely in the database.

Output: Working authentication API with user registration, login, and logout functionality, plus responsive Svelte frontend components.
</objective>

<execution_context>
@/home/boticelli/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/boticelli/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-authentication/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set up Deno backend with Oak framework and authentication routes</name>
  <files>
  src/backend/server.ts
  src/backend/routes/auth.ts
  src/backend/config.ts
  src/backend/deps.ts
  </files>
  <action>
  Create the main Deno backend server using Oak framework with authentication routes. Set up the server configuration with proper middleware for CORS, logging, and error handling. Create authentication routes for registration, login, and logout using the patterns from the research document. Use zod for input validation and jose for JWT handling as recommended in the research. Configure environment variables for JWT secrets and database connection.
  
  In server.ts: Initialize Oak application, configure middleware, set up routes, and implement basic error handling.
  In routes/auth.ts: Create POST endpoints for /auth/register, /auth/login, and /auth/logout with proper validation and response handling.
  In config.ts: Define configuration constants including JWT secrets, database connection strings, and other environment variables.
  In deps.ts: Define imports for Oak, MongoDB, bcrypt, zod, and jose to manage dependencies centrally.
  </action>
  <verify>
  deno run --allow-net --allow-env --allow-read src/backend/server.ts
  </verify>
  <done>
  Server starts successfully, authentication endpoints are defined and accessible
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement user model and database schema for secure user storage</name>
  <files>
  src/backend/models/User.ts
  src/backend/utils/db.ts
  </files>
  <action>
  Create the User model with proper TypeScript interfaces and database operations. Implement methods for user creation, retrieval, and authentication. Ensure passwords are securely hashed using bcrypt as recommended in the research. Set up MongoDB connection utilities with proper error handling and connection pooling. Create proper indexes for email field to ensure uniqueness and fast lookup. Include fields for email, hashed password, creation date, and last login timestamp to meet DB-01 requirement.
  
  In User.ts: Define User interface, create user validation schema, implement user creation with password hashing, implement user retrieval by email, implement password verification method.
  In utils/db.ts: Set up MongoDB connection, handle connection errors, implement proper disconnection logic.
  </action>
  <verify>
  deno run --allow-env --allow-net src/backend/models/User.ts
  </verify>
  <done>
  User model properly defined with secure password hashing, database connection established
  </done>
</task>

<task type="auto">
  <name>Task 3: Build authentication middleware and complete API endpoints</name>
  <files>
  src/backend/middleware/auth.ts
  src/backend/routes/auth.ts
  </files>
  <action>
  Implement authentication middleware that verifies JWT tokens and attaches user information to the request context. Complete the authentication routes with proper error handling, input validation using zod, and secure session management. Implement JWT token generation with short-lived access tokens and longer-lived refresh tokens as recommended in the research document. Ensure all API responses follow consistent format as per API-03 requirement. Include proper status codes (200, 201, 400, 401, 409, 500).
  
  In middleware/auth.ts: Create authMiddleware function that extracts token from Authorization header, verifies JWT, and attaches user info to context.
  In routes/auth.ts: Complete registration with email validation and duplicate checking, implement login with password verification, implement logout functionality, add proper error responses.
  </action>
  <verify>
  curl -X POST http://localhost:8000/auth/register -H "Content-Type: application/json" -d '{"email":"test@example.com","password":"password123"}'
  </verify>
  <done>
  Authentication middleware properly validates tokens, API endpoints return correct responses
  </done>
</task>

</tasks>

<verification>
Test the complete authentication flow:
1. Register a new user via POST /auth/register
2. Log in with the registered credentials via POST /auth/login
3. Verify JWT token is returned and properly formatted
4. Test protected route using authentication middleware
5. Test logout functionality
6. Verify user data is stored securely in database with hashed passwords
</verification>

<success_criteria>
- Users can register with email and password (AUTH-01 satisfied)
- Users can securely log in and maintain sessions (AUTH-02 satisfied)
- Users can log out from any page (AUTH-03 satisfied)
- User data stored securely in database with bcrypt hashing (DB-01 satisfied)
- Authentication API endpoints available (API-03 satisfied)
- Backend server properly configured with Oak framework
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-authentication/01-01-SUMMARY.md`
</output>