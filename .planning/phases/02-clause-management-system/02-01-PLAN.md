---
phase: 02-clause-management-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: 
  - src/backend/models/Clause.ts
  - src/backend/utils/db.ts
  - src/backend/config.ts
  - src/backend/deps.ts
  - src/backend/server.ts
  - src/backend/routes/clauses.ts
autonomous: true
requirements:
  - CLAU-01
  - CLAU-02
  - CLAU-03
  - CLAU-04
  - DB-02
  - API-01

must_haves:
  truths:
    - "System can store clauses with English and Russian text"
    - "Clauses can be tagged for categorization"
    - "Usage metrics are tracked for each clause"
    - "API provides full CRUD operations for clauses"
    - "Database schema supports bilingual clauses with metadata"
  artifacts:
    - path: "src/backend/models/Clause.ts"
      provides: "Clause data model and database schema"
      contains: "Clause interface, database operations, bilingual support"
    - path: "src/backend/routes/clauses.ts"
      provides: "API endpoints for clause CRUD operations"
      exports: ["GET /clauses", "POST /clauses", "PUT /clauses/:id", "DELETE /clauses/:id", "GET /clauses/search"]
    - path: "src/backend/utils/db.ts"
      provides: "Database utilities for clauses"
      contains: "clause collection operations"
  key_links:
    - from: "src/backend/routes/clauses.ts"
      to: "src/backend/models/Clause.ts"
      via: "import and usage"
      pattern: "import.*Clause"
    - from: "src/backend/routes/clauses.ts"
      to: "MongoDB clauses collection"
      via: "database queries"
      pattern: "db\\.collection\\('clauses'\\)"

---

<objective>
Implement the core clause management functionality including database schema and API endpoints.

Purpose: Create the backend infrastructure for storing, retrieving, updating, and deleting clauses with bilingual (English/Russian) support, tagging, and usage tracking as required by CLAU-01 through CLAU-04, DB-02, and API-01.

Output: Working database schema for clauses with bilingual content and metadata, plus API endpoints for full CRUD operations.
</objective>

<execution_context>
@/home/boticelli/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/boticelli/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create clause data model with bilingual support and metadata</name>
  <files>
  src/backend/models/Clause.ts
  src/backend/utils/db.ts
  </files>
  <action>
  Create the Clause model with proper TypeScript interfaces and database operations. The model must support bilingual content with English and Russian text as required by CLAU-01 and CLAU-02. Include fields for searchable tags (CLAU-03), usage count tracking (CLAU-04), and other metadata. Implement methods for clause creation, retrieval, updating, deletion, and searching. Ensure proper indexing for efficient querying of tags and text content. Store clause content in a way that maintains the relationship between English and Russian versions.

  In Clause.ts: Define Clause interface with fields for id, englishText, russianText, tags array, usageCount, createdAt, updatedAt; implement clause validation schema; implement clause creation with proper sanitization; implement retrieval methods (by ID, by tags, full listing); implement update method that increments usage when clause is accessed for building; implement search functionality across both English and Russian text.
  In utils/db.ts: Ensure proper indexing on tags and text fields for efficient search; add any clause-specific database utilities.
  </action>
  <verify>
  deno run --allow-env --allow-net src/backend/models/Clause.ts
  </verify>
  <done>
  Clause model properly defined with bilingual support, tagging, and usage tracking; database indexes set up for efficient queries
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement API endpoints for clause CRUD operations</name>
  <files>
  src/backend/routes/clauses.ts
  src/backend/server.ts
  </files>
  <action>
  Create comprehensive API endpoints for clause management that fulfill the API-01 requirement. Implement GET, POST, PUT, and DELETE operations for full CRUD functionality. Include special endpoints for searching clauses by tags (CLAU-03) and for tracking usage (CLAU-04). Ensure all endpoints return consistent response formats and proper HTTP status codes. Include input validation using zod as established in Phase 1. Protect endpoints that require authentication where appropriate.

  In routes/clauses.ts: Create GET /clauses for listing all clauses with optional filtering; create POST /clauses for adding new clauses with validation; create GET /clauses/:id for retrieving specific clauses; create PUT /clauses/:id for updating clauses; create DELETE /clauses/:id for removing clauses; create GET /clauses/search for searching by text or tags; implement usage tracking endpoint that increments clause usage count.
  In server.ts: Register the new routes with the Oak application router.
  </action>
  <verify>
  curl -X GET http://localhost:8000/clauses
  </verify>
  <done>
  All clause API endpoints implemented and accessible with proper request/response handling
  </done>
</task>

</tasks>

<verification>
Test the complete clause management API:
1. Create a new clause via POST /clauses with English and Russian text
2. Retrieve the clause via GET /clauses/:id
3. List all clauses via GET /clauses
4. Update the clause via PUT /clauses/:id
5. Search clauses by tags via GET /clauses/search?tags=sample
6. Verify usage count updates when clauses are accessed
7. Delete the clause via DELETE /clauses/:id
8. Verify database schema properly stores bilingual content and metadata
</verification>

<success_criteria>
- Clauses can store English text content (CLAU-01 satisfied)
- Clauses can store Russian text content (CLAU-02 satisfied) 
- Clauses have searchable tags for categorization (CLAU-03 satisfied)
- Clauses track usage count for popularity metrics (CLAU-04 satisfied)
- Database stores clauses with English and Russian texts (DB-02 satisfied)
- API provides endpoints for clause CRUD operations (API-01 satisfied)
- Backend server properly configured with clause routes
</success_criteria>

<output>
After completion, create `.planning/phases/02-clause-management-system/02-01-SUMMARY.md`
</output>